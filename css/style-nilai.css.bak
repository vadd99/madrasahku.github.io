/* Tambahan CSS khusus Rekap Nilai */

.btn-tambah-uh {
    background: transparent;
    color: #10b981;
    border: 1px dashed #10b981;
    padding: 10px;
    width: 100%;
    border-radius: var(--radius-sm, 6px);
    cursor: pointer;
    font-weight: 600;
    margin-top: 15px;
    transition: all 0.2s ease;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 8px;
}

.btn-tambah-uh:hover {
    background: rgba(16, 185, 129, 0.1);
}

.btn-primary {
    background-color: #10b981;
    color: white;
    border: none;
    cursor: pointer;
    transition: 0.2s;
}

.btn-primary:hover {
    background-color: #059669;
}

.badge-status-empty { 
    background: rgba(255, 255, 255, 0.06); 
    color: var(--text-muted); 
    border: 1px dashed var(--border-color); 
}

/* === PERBAIKAN LAYOUT SELECTOR (1 BARIS BAGI 2) === */
.selectors-container {
    display: grid !important;
    grid-template-columns: 1fr 1fr; /* Membagi lurus 50% - 50% */
    gap: 12px !important;
    width: 100%;
    margin-top: 0 !important; 
    margin-bottom: 16px; /* Jarak ke list santri */
}

.imtihan-selector-wrapper {
    display: flex;
    flex-direction: column; /* Label dipindah ke atas kotak input */
    align-items: flex-start;
    width: 100%; 
    box-sizing: border-box;
    gap: 6px;
    margin-top: 0;
}

.imtihan-selector-wrapper label {
    display: flex;
    align-items: center;
    gap: 4px;
    margin: 0;
    font-size: 0.75rem; /* Dikecilkan sedikit agar rapi */
    font-weight: 600;
    color: var(--text-muted); 
}

.imtihan-selector-wrapper select {
    width: 100%;
    padding: 8px 10px; /* Ukuran input dibuat ideal untuk ditekan */
    font-size: 0.85rem;
}

/* === TOMBOL CLOSE UNTUK PANDUAN PENILAIAN === */
.absensi-guide-box {
    position: relative;
    padding-right: 40px; 
}

.btn-close-guide {
    position: absolute;
    top: 12px;
    right: 12px;
    background: transparent;
    border: none;
    color: var(--text-muted);
    cursor: pointer;
    padding: 4px;
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: 0.2s;
}

.btn-close-guide:hover {
    background: rgba(239, 68, 68, 0.15); 
    color: #ef4444;
}

/* === PERBAIKAN TAMPILAN REKAP NILAI (MINI BADGES) === */
.nilai-badge-container {
    display: flex;
    flex-wrap: wrap; 
    gap: 4px;
    justify-content: flex-start; 
    max-width: 180px; 
}

.nilai-mini-badge {
    background: rgba(16, 185, 129, 0.15); 
    color: #10b981; 
    border: 1px solid rgba(16, 185, 129, 0.3);
    padding: 2px 6px;
    border-radius: 4px;
    font-size: 0.65rem;
    white-space: nowrap; 
}

/* === STYLING HEADER "REKAP NILAI" BIAR LEBIH SLIM (SUPPORT LIGHT/DARK) === */
.page-header-card {
    background: linear-gradient(145deg, rgba(16, 185, 129, 0.05) 0%, #ffffff 100%);
    border: 1px solid rgba(16, 185, 129, 0.2);
    border-radius: 10px;
    padding: 12px; /* Padat */
    margin-bottom: 12px; 
    position: relative;
    overflow: hidden;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.03);
}

body.dark .page-header-card {
    background: linear-gradient(145deg, rgba(16, 185, 129, 0.08) 0%, rgba(0, 0, 0, 0.2) 100%);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
}

.page-header-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 3px;
    background: linear-gradient(90deg, #10b981, transparent);
    opacity: 0.9;
}

.header-content-flex {
    display: flex;
    align-items: center;
    gap: 12px; /* Jarak lebih rapat */
}

/* Kotak ikon bernuansa hijau */
.header-icon-wrapper {
    background: rgba(16, 185, 129, 0.15);
    color: #10b981;
    width: 38px; /* Diperkecil */
    height: 38px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    border: 1px solid rgba(16, 185, 129, 0.3);
}

.header-icon-wrapper i {
    width: 20px;
    height: 20px;
}

/* Merapikan teks judul dan subjudul */
.header-text-wrapper .page-title {
    font-size: 1.1rem; /* Diperkecil */
    font-weight: 700;
    color: #1e293b; /* Warna gelap untuk Light Mode */
    margin: 0 0 2px 0;
}

body.dark .header-text-wrapper .page-title {
    color: #ffffff; /* Warna putih untuk Dark Mode */
}

.header-text-wrapper .page-subtitle {
    font-size: 0.75rem; /* Diperkecil */
    color: #64748b; /* Abu-abu untuk Light Mode */
    margin: 0;
    line-height: 1.3;
}

body.dark .header-text-wrapper .page-subtitle {
    color: #94a3b8; /* Abu-abu terang untuk Dark Mode */
}

/* === PERBAIKAN TOMBOL BACK JADI IKON MINIMALIS === */
#btn-back-kelas {
    background: rgba(16, 185, 129, 0.15); 
    color: #10b981;
    border: 1px solid rgba(16, 185, 129, 0.3);
    width: 38px;
    height: 38px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    margin: 0; 
    cursor: pointer;
    transition: all 0.2s;
    flex-shrink: 0;
}

#btn-back-kelas:hover {
    background: #10b981;
    color: #ffffff;
}

#btn-back-kelas i {
    width: 20px;
    height: 20px;
}

/* Memaksa selector hilang jika ada class hidden dari JS */
.selectors-container.hidden {
    display: none !important;
}

/* ===================================================
   STYLING CARD KELAS BARU YANG LEBIH MENARIK
   =================================================== */
.kelas-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 14px;
    padding-bottom: 20px;
}

.kelas-card-item {
    display: flex;
    align-items: center;
    background: #ffffff; /* Putih bersih untuk Light Mode */
    border: 1px solid #e2e8f0;
    border-radius: 14px;
    padding: 16px;
    cursor: pointer;
    transition: all 0.3s ease;
    gap: 16px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.04);
}

body.dark .kelas-card-item {
    background: linear-gradient(145deg, rgba(30, 41, 59, 0.7) 0%, rgba(15, 23, 42, 0.9) 100%);
    border: 1px solid rgba(255, 255, 255, 0.08);
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
}

.kelas-card-item:hover {
    transform: translateY(-3px);
    border-color: rgba(16, 185, 129, 0.5);
    box-shadow: 0 8px 20px rgba(16, 185, 129, 0.1);
    background: #f8fafc;
}

body.dark .kelas-card-item:hover {
    background: linear-gradient(145deg, rgba(30, 41, 59, 0.9) 0%, rgba(15, 23, 42, 1) 100%);
    box-shadow: 0 8px 20px rgba(16, 185, 129, 0.15);
}

/* Ikon Buku di sebelah kiri */
.kelas-icon-wrapper {
    background: rgba(16, 185, 129, 0.15);
    color: #10b981;
    width: 50px;
    height: 50px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    border: 1px solid rgba(16, 185, 129, 0.3);
}

.kelas-icon-wrapper i {
    width: 26px;
    height: 26px;
}

.kelas-info {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 8px;
    min-width: 0; /* Mencegah overflow ke samping */
}

.kelas-name {
    font-size: 1.15rem;
    font-weight: 700;
    color: #1e293b; /* Gelap untuk Light Mode */
    margin: 0;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

body.dark .kelas-name {
    color: #ffffff;
}

/* Wadah untuk badge (Bungkus Teks) */
.kelas-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
}

.meta-badge {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    font-size: 0.7rem;
    font-weight: 600;
    padding: 4px 10px;
    border-radius: 20px;
    white-space: nowrap;
}

.meta-badge i {
    width: 12px;
    height: 12px;
}

/* Pembungkus Teks Wali Kelas */
.meta-wali {
    background: rgba(56, 189, 248, 0.15);
    color: #0284c7; /* Biru agak gelap di Light Mode */
    border: 1px solid rgba(56, 189, 248, 0.3);
}

body.dark .meta-wali {
    color: #38bdf8; /* Biru terang di Dark Mode */
}

/* Pembungkus Teks Jumlah Santri */
.meta-santri {
    background: rgba(244, 63, 94, 0.15);
    color: #e11d48; /* Pink agak gelap di Light Mode */
    border: 1px solid rgba(244, 63, 94, 0.3);
}

body.dark .meta-santri {
    color: #f43f5e; /* Pink terang di Dark Mode */
}

/* Tombol Anak Panah Kanan */
.kelas-action {
    display: flex;
    align-items: center;
    justify-content: center;
    color: #64748b;
    transition: all 0.3s ease;
    background: #f1f5f9;
    width: 32px;
    height: 32px;
    border-radius: 50%;
}

body.dark .kelas-action {
    color: #94a3b8;
    background: rgba(255, 255, 255, 0.05);
}

.kelas-card-item:hover .kelas-action {
    color: #ffffff;
    background: #10b981;
    transform: translateX(3px); /* Memberikan efek geser tipis saat dihover */
}

.kelas-action i {
    width: 18px;
    height: 18px;
}
